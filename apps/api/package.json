{
  "name": "@gaffer/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "source .venv/bin/activate && uvicorn app.main:app --reload --port 8000",
    "deploy": "fly deploy"
  }
}
